fastapi>=0.100.0
uvicorn[standard]>=0.23.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
pydantic[email]>=2.0.0
psycopg2-binary>=2.9.6 # Postgres driver
passlib[bcrypt]>=1.7.4 # For hashing
python-jose[cryptography]>=3.3.0 # For JWT
alembic>=1.11.1 # Database migrations
# Depending on how the shared module behaves, standard python lib additions like 'uuid' or 'datetime' are built-in.
